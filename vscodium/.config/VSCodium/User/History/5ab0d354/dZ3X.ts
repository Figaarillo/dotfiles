const numberList: number[] = Array.from(new Array(100), () => Math.round(Math.random() * 10))

const sequenceOfValues = (numberList: Array<number>): void => {

  const min = numberList.reduce((acc, curr, i) => {
    
  },0)
}

sequenceOfValues(numberList)