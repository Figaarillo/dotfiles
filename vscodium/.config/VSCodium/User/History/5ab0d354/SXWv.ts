const numberList: number[] = Array.from(new Array(100), () => Math.round(Math.random() * 10))

const sequenceOfValues = (numList: Array<number>): void => {

  console.log(numberList)
}

sequenceOfValues(10)