const vector1: number[] = Array.from(new Array(10), () => Math.round(Math.random() *10))
const vector2: number[] = Array.from(new Array(10), () => Math.round(Math.random() *10))

const addVectors = (vector1: number[], vector2: number[]) => {
  
}